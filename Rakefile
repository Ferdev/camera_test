# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project'

Motion::Project::App.setup do |app|
  app.name = 'camera_test'

  app.deployment_target = '5.0'

  app.frameworks << 'AVFoundation'
end
